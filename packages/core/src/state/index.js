import { appState as defaultAppState } from './default'
import { appState as mockAppState } from './mock'

export const state = {
  default: {
    appState: defaultAppState,
  },
  mock: {
    appState: mockAppState,
  },
}
